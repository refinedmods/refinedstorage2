name: Publish release
on:
  pull_request:
    branches:
      - support/2.x
    types:
      - closed
jobs:
  publish-release:
    uses: refinedmods/refinedarchitect/.github/workflows/publish-release.yml@v1.4.1
    secrets: inherit
    with:
      project-name: 'Refined Storage'
      gametests: true
      curseforge: true
      modrinth: true
